{
    "version": 4,
    "cmakeMinimumRequired": {
        "major": 3,
        "minor": 23,
        "patch": 0
    },
    "configurePresets": [
        {
            "name": "debug",
            "displayName": "Debug Build",
            "description": "Configure to build with debug symbols. Recommended for this assignment.",
            "binaryDir": "${sourceDir}/build",
            "cacheVariables": {
                "CMAKE_BUILD_TYPE": "Debug",
                "CMAKE_EXPORT_COMPILE_COMMANDS": true
            }
        }
    ],
    "buildPresets": [
        {
            "name": "Default - Runs like normal",
            "configurePreset": "debug",
            "displayName": "Build All (Debug)",
            "description": "Build all targets in the debug configuration."
        }
    ],
    "testPresets": [
        {
            "name": "Tests - Only runs the tests to make sure all tasks completed.",
            "configurePreset": "debug",
            "output": {
                "outputOnFailure": true
            },
            "execution": {
                "noTestsAction": "error"
            }
        }
    ]
}
